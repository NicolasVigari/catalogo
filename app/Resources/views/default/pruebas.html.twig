{# empty Twig template #}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}
	
{% for product in products %}
		{{ product.name }} <br>
	{% endfor %}
	
